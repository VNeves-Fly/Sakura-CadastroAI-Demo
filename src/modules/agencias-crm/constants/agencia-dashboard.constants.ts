// Cores dos gráficos da aba Vendas do modal de Agência — mesmos tokens
// globais usados nos dashboards de Executivo/Gestor (globals.css
// --chart-1..4), pra manter todos os módulos de CRM com a mesma paleta
// (nunca aparentar que cada módulo ficou de um jeito). Duplicado de
// propósito — mesmo princípio de isolamento entre módulos já documentado
// no resto do projeto.
export const COR_AEREO_NACIONAL = "hsl(var(--chart-1))";
export const COR_AEREO_INTERNACIONAL = "hsl(var(--chart-2))";
export const COR_TERRESTRE = "hsl(var(--chart-3))";
export const COR_ALERTA = "hsl(var(--chart-4))";
